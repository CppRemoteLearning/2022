cmake_minimum_required(VERSION 3.0.0)
project(tema2 VERSION 0.1.0)
include(CTest)
enable_testing()
project(2022)
include_directories(headers)
file(GLOB SOURCES "*.cpp")
add_executable(tema2 ${SOURCES})
include(CPack)
